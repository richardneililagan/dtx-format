# Parse snapshots

Empty for now. `dtxmania-poly`'s golden parse snapshots (`corpus/golden/`) move
here alongside the charts they describe — see the mapping table in the root
[README](../README.md).

Held to the same rules as the charts: generated, but never normalised and never
edited by hand to make a test pass. A changed snapshot is a change in what a
player hears.
